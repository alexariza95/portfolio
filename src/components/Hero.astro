---
// Creating a high-impact Hero component
---

<section
  id="hero-section"
  class="relative bg-dark text-white overflow-hidden min-h-screen flex items-center justify-center"
>
  <!-- Dynamic Background with Gradient Mesh -->
  <div class="absolute inset-0 bg-dark pointer-events-none overflow-hidden">
    <div
      class="absolute top-[10%] left-[20%] w-[500px] h-[500px] bg-primary/20 blur-[120px] rounded-full mix-blend-screen opacity-30 animate-blob"
    >
    </div>
    <div
      class="absolute top-[10%] right-[20%] w-[500px] h-[500px] bg-secondary/20 blur-[120px] rounded-full mix-blend-screen opacity-30 animate-blob animation-delay-2000"
    >
    </div>
    <div
      class="absolute bottom-[20%] left-[50%] transform -translate-x-1/2 w-[500px] h-[500px] bg-purple-500/20 blur-[120px] rounded-full mix-blend-screen opacity-30 animate-blob animation-delay-4000"
    >
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-8 text-center">
    <div
      class="backdrop-blur-sm bg-dark/10 rounded-3xl p-8 md:p-12 border border-white/5 shadow-2xl shadow-black/20 animate-on-scroll"
    >
      <div class="space-y-8">
        <h2
          class="text-base md:text-lg font-semibold tracking-widest text-primary uppercase animate-fade-in-up"
          style="animation-duration: 0.6s;"
        >
          FULL STACK ENGINEERING
        </h2>

        <h1
          class="font-display text-5xl md:text-7xl lg:text-8xl font-bold tracking-tight leading-tight animate-fade-in-up drop-shadow-2xl"
          style="animation-duration: 0.8s;"
        >
          <span class="block text-white mb-2">Complex Problems.</span>
          <span
            class="block bg-linear-to-r from-primary via-purple-400 to-secondary bg-clip-text text-transparent pb-4"
            >Elegant Solutions</span
          >
        </h1>

        <p
          class="mt-6 max-w-2xl mx-auto text-lg md:text-xl text-gray-200 font-normal leading-relaxed animate-fade-in-up drop-shadow-md"
          style="animation-duration: 1s;"
        >
          I’m Alex Ariza, a Software Engineer focused on scalability and user
          experience. I translate business goals into high-performance web
          applications that simply work.
        </p>

        <div
          class="mt-10 flex flex-col sm:flex-row gap-5 justify-center items-center animate-fade-in-up"
          style="animation-duration: 1.2s;"
        >
          <a
            href="#experience"
            class="px-8 py-4 bg-white text-dark font-bold rounded-full hover:bg-gray-100 hover:scale-105 transition-all duration-300 shadow-xl shadow-white/10"
          >
            My Journey
          </a>
          <a
            href="/docs/Alex_Ariza_SSE.pdf"
            download="Alex_Ariza_Senior_Software_Engineer.pdf"
            target="_blank"
            rel="noopener noreferrer"
            class="px-8 py-4 bg-transparent border-2 border-white/20 text-white font-medium rounded-full hover:border-white/40 hover:bg-white/5 hover:scale-105 transition-all duration-300 backdrop-blur-md"
          >
            Download CV
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  #hero-section {
    view-timeline-name: --hero;
  }
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }
  .animate-blob {
    animation: blob 7s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  .animation-delay-4000 {
    animation-delay: 4s;
  }

  /* Scroll Driven Animation */
  @keyframes hero-exit {
    to {
      transform: translateY(-50px) scale(0.9);
      opacity: 0;
      filter: blur(10px);
    }
  }

  @media (prefers-reduced-motion: no-preference) {
    .animate-on-scroll {
      animation: hero-exit linear forwards;
      animation-timeline: --hero;
      animation-range: exit 0% exit 50%;
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }
</style>
